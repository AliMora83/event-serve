name: Generate PROJECT-SYNC.json

on:
  push:
    branches:
      - main
      - master

permissions:
  contents: write

jobs:
  generate-project-sync:
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate PROJECT-SYNC.json
        run: |
          TIMESTAMP=$(date -u '+%Y-%m-%dT%H:%M:%SZ')
          TODAY=$(date -u '+%Y-%m-%d')
          ACTOR="${{ github.actor }}"
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          SHORT_SHA="${{ github.sha }}"
          SHORT_SHA="${SHORT_SHA:0:7}"
          VERSION=$(grep -oP 'Version: \K[0-9]+\.[0-9]+\.[0-9]+' Master.md 2>/dev/null || echo "0.0.0")

          cat > PROJECT-SYNC.json << ENDJSON
          {
            "project": "Event-Serve",
            "repo": "AliMora83/Events-Website",
            "branch": "main",
            "stack": "TypeScript / Vite / Tailwind / PDF.js",
            "status": "Active",
            "priority": 2,
            "priority_label": "🟡 Priority 2 — Active Development",
            "progress_percent": 55,
            "progress_label": "Pass generation active · QR scanning integration in dev",
            "current_phase": "Phase 2 — Check-in Logic",
            "next_step": "Implement real-time check-in sync with EventSaaS API",
            "blocker": null,
            "live_url": "https://serve.namka.org",
            "deploy_target": "Hostinger VPS (Nginx)",
            "agents": ["Antigravity"],
            "version": "$VERSION",
            "last_push": {
              "timestamp": "$TIMESTAMP",
              "actor": "$ACTOR",
              "commit_message": "$COMMIT_MSG",
              "sha": "$SHORT_SHA"
            },
            "last_updated": "$TODAY"
          }
          ENDJSON

          sed -i 's/^          //' PROJECT-SYNC.json
          echo "PROJECT-SYNC.json generated."

      - name: Commit PROJECT-SYNC.json
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git diff --quiet PROJECT-SYNC.json || (
            git add PROJECT-SYNC.json &&
            git commit -m "chore: update PROJECT-SYNC.json [skip ci]" &&
            git push
          )
